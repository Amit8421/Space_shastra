Place your Space Shastra Interiors logo image here as `logo.png`.

This file is only a placeholder to keep the public folder in version control.

The app layout references `/logo.png` in `src/app/layout.tsx`.
